services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - .:/app
    command: python app.py

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
